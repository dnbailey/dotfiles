#!/usr/bin/env bash

# Navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias -- -="cd -"

# Shortcuts
alias dl="cd ~/Downloads"
alias dt="cd ~/Desktop"
alias dc="cd ~/code && ls"
alias c="clear"

# Git Shortcuts
alias g="git"
alias gs="git status"
alias ga="git add -A"
